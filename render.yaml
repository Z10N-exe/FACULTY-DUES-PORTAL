services:
  # ── Backend API (Free Web Service) ──────────────────────────
  - type: web
    name: faculty-dues-api
    runtime: node
    plan: free
    rootDir: server
    buildCommand: npm install
    startCommand: node index.js
    envVars:
      - key: PORT
        value: 10000
      - key: MONGO_URI
        sync: false
      - key: PAYSTACK_SECRET_KEY
        sync: false
      - key: JWT_SECRET
        value: supersecret123
      - key: CLIENT_URL
        value: https://faculty-dues-portal.onrender.com

  # ── Student Frontend (Free Static Site) ─────────────────────
  - type: web
    name: faculty-dues-client
    runtime: static
    rootDir: client
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    headers:
      - path: /*
        name: Cache-Control
        value: no-cache
    routes:
      - type: rewrite
        source: /*
        destination: /index.html

  # ── Admin Panel (Free Static Site) ──────────────────────────
  - type: web
    name: faculty-dues-admin
    runtime: static
    rootDir: admin
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    headers:
      - path: /*
        name: Cache-Control
        value: no-cache
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
